# Frontend Environment Variables

# API Base URL (backend API)
# For local development, this is typically http://localhost:5000
# For Docker, this would be http://localhost:5000 (accessing from host machine)
# or http://backend:8080 (if frontend is also in Docker and using container networking)
VITE_API_BASE_URL=http://localhost:5000
